"use client";

import { ReactNode } from "react";

import ReactECharts from "echarts-for-react";

export default function Chart11(): ReactNode {
  let option;

  return <ReactECharts option={option} />;
}
